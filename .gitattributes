# Treat the LTSM WASM module as binary (never diff/merge as text).
okline/ltsm/ltsm.wasm binary

# Normalise line endings for text sources.
*.py text eol=lf
*.md text eol=lf
*.toml text eol=lf
*.txt text eol=lf
*.js text eol=lf
